<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="./styles/styles.css">
</head>
<body>
    

    <div class="contenedor"></div>

    <hr>

    <header>
        <% if(v == 0){ %>
            <h1 class="text-center">Register</h1>
        <% }else{ %>
            <h1 class="text-center">Login</h1>
        <% } %>
    </header>

    <main>
        <div class="container">
            <form class="form-control" style="display: flex; flex-direction: column;" id="form">
                <input type="text" placeholder="nombre" style="width: 400px; padding: 6px; margin: 15px auto;" id="nombre" required>
                <% if(v == 0){ %>
                <input type="text" placeholder="cedula" style="width: 400px; padding: 6px; margin: 15px auto;" id="cedula" required>
                <%  } %>
                <input type="password" placeholder="clave" style="width: 400px; padding: 6px; margin: 15px auto;" id="clave" required>
                <div style="width: 500px; margin: auto;" class="d-flex justify-content-center">
                    <button class="btn btn-outline-primary" style="max-width: 200px; margin: auto;">Enviar</button>
                    <% if(v != 0) {%>
                        <a href="/register" style="text-decoration: none; max-width: 200px; margin: auto;">Registrarse</a>
                    <% }else{ %>
                        <a href="/login" style="text-decoration: none; max-width: 200px; margin: auto;">Inciar sesión</a>
                    <% } %>
                </div>
            </form>
        </div>
    </main>

    <div class="contenedor"></div>
   
    <footer>
        <div class="text-center" style="display: flex; flex-direction: column;">
            <span>&copy; Todos los derechos reservados</span>
            <span>Creador: Roberto Ducharne</span>
            <span>Contactos</span>
            <span>Gmail: rducharne2@gmail.com</span>
            <span>Número: 04248907707</span>
        </div>
    </footer>

    <script type="module" src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        const v = "<%= v %>";
    </script>
    <script src="./js/app.js"></script>
</body>
</html>